<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_log_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".storage.message.group.MessageGroupFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/message_groups_recyclerview"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="4dp"
        android:scrollbars="vertical"
        app:layout_constraintBottom_toTopOf="@+id/message_groups_filter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:listitem="@layout/activity_message_groups_view_item" />

    <androidx.appcompat.widget.AppCompatEditText
        android:id="@+id/message_groups_filter"
        android:layout_width="0dp"
        android:maxLength="32"
        android:layout_height="wrap_content"
        android:hint="@string/message_groups_filter_hint"
        android:inputType="textCapCharacters"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/message_groups_recyclerview" />

</androidx.constraintlayout.widget.ConstraintLayout>